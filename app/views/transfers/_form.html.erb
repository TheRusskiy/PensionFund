<%= form_for(@transfer) do |f| %>
  <% if @transfer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transfer.errors.count, "error") %> prohibited this transfer from being saved:</h2>

      <ul>
      <% @transfer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :company, t('transfer.company') %><br>
    <%= f.number_field :company %>
  </div>
  <div class="field">
    <%= f.label :transfer_date, t('transfer.transfer_date') %><br>
    <%= f.date_select :transfer_date %>
  </div>
  <div class="field">
    <%= f.label :amount, t('transfer.amount') %><br>
    <%= f.number_field :amount %>
  </div>
  <div class="field">
    <%= f.label :month, t('transfer.month') %><br>
    <%= f.number_field :month %>
  </div>
  <div class="field">
    <%= f.label :year, t('transfer.year') %><br>
    <%= f.number_field :year %>
  </div>
  <div class="actions">
    <%= f.submit t('transfer.update') %>
  </div>
<% end %>
