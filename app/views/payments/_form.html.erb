<%= form_for(@payment) do |f| %>
  <% if @payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payment.errors.count, "error") %> prohibited this payment from being saved:</h2>

      <ul>
      <% @payment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :company_id, t('payment.company') %><br>
    <%= f.number_field :company_id %>
  </div>
  <div class="field">
    <%= f.label :employee_id, t('payment.employee') %><br>
    <%= f.text_field :employee_id %>
  </div>
  <div class="field">
    <%= f.label :year, t('payment.year') %><br>
    <%= f.number_field :year %>
  </div>
  <div class="field">
    <%= f.label :month, t('payment.month') %><br>
    <%= f.number_field :month %>
  </div>
  <div class="field">
    <%= f.label :amount, t('payment.amount') %><br>
    <%= f.number_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit t('payment.update') %>
  </div>
<% end %>
